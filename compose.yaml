services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: weather-bot
    env_file: .env
    init: true
    restart: unless-stopped
    command: ["python", "-u", "bot.py"]
